# Example environment variables required by CI/CD
# Copy this file to `.env` and fill in values as needed.
# These map to GitHub Secrets referenced in `.github/workflows/deploy-cloudflare.yml`.

# Cloudflare
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# GitHub
# Personal access token or repo-scoped token used by the workflow to update repo metadata and post comments
ADMIN_TOKEN=
