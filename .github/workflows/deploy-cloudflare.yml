name: Deploy to Cloudflare Pages and Update Homepage

on:
  push:
    branches: [main, master]
    paths:
      - 'app/**'
      - 'public/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'vite.config.ts'
      - 'tsconfig.json'
      - 'tailwind.config.*'
      - '.github/workflows/deploy-cloudflare.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Cloudflare Pages
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Remix SPA
        run: pnpm build

      - name: Ensure SPA fallback (_redirects)
        run: |
          if [ -f public/_redirects ]; then
            mkdir -p build/client
            cp public/_redirects build/client/_redirects
          fi

      - name: Create Cloudflare Pages Project if not exists
        run: |
          # Derive a valid project slug from repo name
          BASE_NAME="${{ github.event.repository.name }}"
          BASE_SLUG=$(echo "$BASE_NAME" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9-]+/-/g' | sed -E 's/^-+|-+$//g')
          CANDIDATE="$BASE_SLUG"

          echo "Attempting to create Cloudflare Pages project: $CANDIDATE"
          CREATE_RESPONSE=$(curl -s -w "%{http_code}" -o /tmp/cf_create_response.json \
            -X POST "https://api.cloudflare.com/client/v4/accounts/${{ secrets.CLOUDFLARE_ACCOUNT_ID }}/pages/projects" \
            -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"name\": \"${CANDIDATE}\",
              \"production_branch\": \"main\",
              \"build_config\": {
                \"build_command\": \"pnpm build\",
                \"destination_dir\": \"build/client\",
                \"root_dir\": \".\"
              }
            }")

          CREATE_HTTP_CODE=${CREATE_RESPONSE: -3}

          if [ "$CREATE_HTTP_CODE" = "200" ] || [ "$CREATE_HTTP_CODE" = "201" ]; then
            echo "✅ Created Cloudflare Pages project: ${CANDIDATE}"
            echo "PROJECT_NAME=${CANDIDATE}" >> "$GITHUB_ENV"
            cat /tmp/cf_create_response.json | jq '.result.subdomain' || true
          else
            # Read body for context
            BODY=$(cat /tmp/cf_create_response.json || true)
            echo "Creation failed ($CREATE_HTTP_CODE). Response: $BODY"

            # If already exists (some APIs may return 409 or 400 with specific error), proceed
            if echo "$BODY" | grep -qi "already exists"; then
              echo "ℹ️ Project appears to already exist; proceeding with existing name"
              echo "PROJECT_NAME=${CANDIDATE}" >> "$GITHUB_ENV"
            elif [ "$CREATE_HTTP_CODE" = "409" ]; then
              echo "ℹ️ Project already exists (409). Proceeding."
              echo "PROJECT_NAME=${CANDIDATE}" >> "$GITHUB_ENV"
            else
              # Fallback to a unique suffix (short SHA)
              FALLBACK="${BASE_SLUG}-${GITHUB_SHA:0:7}"
              echo "Trying fallback name: $FALLBACK"
              CREATE_FB=$(curl -s -w "%{http_code}" -o /tmp/cf_create_fb.json \
                -X POST "https://api.cloudflare.com/client/v4/accounts/${{ secrets.CLOUDFLARE_ACCOUNT_ID }}/pages/projects" \
                -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
                -H "Content-Type: application/json" \
                -d "{
                  \"name\": \"${FALLBACK}\",
                  \"production_branch\": \"main\",
                  \"build_config\": {
                    \"build_command\": \"pnpm build\",
                    \"destination_dir\": \"build/client\",
                    \"root_dir\": \".\"
                  }
                }")
              CREATE_FB_CODE=${CREATE_FB: -3}
              if [ "$CREATE_FB_CODE" = "200" ] || [ "$CREATE_FB_CODE" = "201" ] || [ "$CREATE_FB_CODE" = "409" ]; then
                echo "✅ Using project name: ${FALLBACK}"
                echo "PROJECT_NAME=${FALLBACK}" >> "$GITHUB_ENV"
                cat /tmp/cf_create_fb.json | jq '.result.subdomain' || true
              else
                echo "❌ Failed to create fallback project. HTTP Code: $CREATE_FB_CODE"; cat /tmp/cf_create_fb.json || true; exit 1
              fi
            fi
          fi

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        id: cloudflare-deploy
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: ${{ env.PROJECT_NAME }}
          directory: build/client
          gitHubToken: ${{ secrets.ADMIN_TOKEN }}
          wranglerVersion: '3'

      - name: Update Repository Homepage URL
        if: success()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.ADMIN_TOKEN }}
          script: |
            const deploymentUrl = '${{ steps.cloudflare-deploy.outputs.url }}';
            
            if (deploymentUrl && deploymentUrl !== '') {
              try {
                await github.rest.repos.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  homepage: deploymentUrl,
                  description: 'Remix SPA – Cloudflare Pages static deployment'
                });
                
                console.log(`✅ Repository homepage updated to: ${deploymentUrl}`);
              } catch (error) {
                console.error('❌ Failed to update repository homepage:', error);
                core.setFailed(`Failed to update homepage: ${error.message}`);
              }
            } else {
              console.log('⚠️ No deployment URL found, skipping homepage update');
            }

      - name: Comment on PR with deployment URL
        if: github.event_name == 'pull_request' && success()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.ADMIN_TOKEN }}
          script: |
            const deploymentUrl = '${{ steps.cloudflare-deploy.outputs.url }}';
            
            if (deploymentUrl && deploymentUrl !== '') {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: `🚀 **Cloudflare Pages Deployment**\n\n✅ Successfully deployed to: ${deploymentUrl}\n\n📝 Preview your changes at the link above!`
              });
            }

      - name: Create GitHub deployment
        if: always()
        id: create-deployment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.ADMIN_TOKEN }}
          script: |
            const deployment = await github.rest.repos.createDeployment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: context.sha,
              environment: 'production',
              description: 'Cloudflare Pages deployment',
              auto_merge: false,
              required_contexts: []
            });
            
            return deployment.data.id;

      - name: Update deployment status
        if: always()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.ADMIN_TOKEN }}
          script: |
            const deploymentUrl = '${{ steps.cloudflare-deploy.outputs.url }}';
            const state = '${{ job.status }}' === 'success' ? 'success' : 'failure';
            const deploymentId = ${{ steps.create-deployment.outputs.result }};
            
            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: deploymentId,
              state: state,
              environment_url: deploymentUrl || '',
              description: state === 'success' ? 'Deployment succeeded' : 'Deployment failed'
            });
